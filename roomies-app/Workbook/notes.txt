Nothing is showing up on the page
Continue with Google, starts to load the page, but then does nothing after that, just returns to the sign in page. Fix that.
Add a show password button in the password field, use the little eye symbol that every website has.
The authenticator website (Sign in with AppWare) will redirect the user to whatever app you're trying to use.
When I try to make an account, I'm getting an error that says "Database error saving new user"
If the user enters their sign in information, but realizes they don't have an account and then hit the create account button, the text fields should clear out.
Take out sign in with Google and sign in with apple cards.
Cursor on phone at first shows up outside the email textbox and then returns.
Console error when I try to make an account: Failed to load resource: the server responded with a status of 500 ()
The iOS cursor glitch is still happening
Add a message on top of the log "Sign in with AppWare" button that says "Sync your AppWare apps!"
Execute Tutorial.md — build a tutorial after onboarding, first time only, with rerun in settings.
Sign in with AppWare takes the user to Site not found. it should be routing to authappware.netlify.app
The iOS cursor on the sign in page is still being really weird.
When I try to make an account, I get a console error: Failed to load resource https://qugmexzeqftqydrbecxd.supabase.co/auth/v1/signup: the server responded with a status of 500 (). (API keys are set in Netlify env vars.)
The tutorial shouldn't just be cards, but should take the user through each feature by having them tap different places and showing them the feature with a little message next to it.
I'm still getting that exact same error as before when I try to create an account with Roomies (500 on /auth/v1/signup)
I emptied .gitignore, I wanted all files and dependencies to follow me from machine to machine.
Netlify build error: #L52-L58 shows TypeScript error TS2551 because supabase.from('profiles').insert(...) returns a PostgrestFilterBuilder, which does not expose a .catch() method. Chaining .catch on the awaited call therefore fails the build.
Making an account gives the same database save error as it has been.
Sigining with  AppWare does not sign me into the app, it just brings right back to the log in screen.
Now Roomies logs in, but doesn't make it past the initial loading page
Please think properly and fix both of the below bugs, I've asked you to fix them like 4 times now:
ROOMIES STILL ISN'T WORKING, IT LOADS AND THEN GOES RIGHT BACK TO THE LOG IN SCREEN AFTER LOGGING IN WITH APPWARE
Creating a new account is still doing the exact same thing, error "Database error saving new error".
